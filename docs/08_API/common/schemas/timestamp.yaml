components:
  schemas:
    Timestamp:
      type: string
      format: date-time
      description: ISO 8601 UTC timestamp.

    UUID:
      type: string
      format: uuid
      description: RFC 4122 UUID identifier.
