import type { WorkspacePackageName } from '@gmrlog/types';

/**
 * UI package marker — design system tokens + RN primitives (D3.1).
 */
export const uiPackageName = 'ui' as const satisfies WorkspacePackageName;
