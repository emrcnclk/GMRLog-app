/**
 * D3.28 (PRODUCT_POLISH_AUDIT F11) — Discover carried its own, lesser copy of this
 * card: no joined-state badge, no avatar image, no press feedback. Two renderings
 * of one entity is one too many, so Discover now uses the communities original.
 */
export {
  CommunityCard,
  type CommunityCardProps,
} from '../../communities/components/community-card';
