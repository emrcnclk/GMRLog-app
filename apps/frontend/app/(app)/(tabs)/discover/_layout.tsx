import { Stack } from 'expo-router';

/** Discover stack — hub + games / communities / events / D3.22 module children. */
export default function DiscoverLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
