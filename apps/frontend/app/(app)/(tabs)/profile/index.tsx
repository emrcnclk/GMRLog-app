import { ProfileScreen } from '../../../../features/profile';

/** Profile tab — production identity · library · cultural surfaces. */
export default function ProfileTabRoute() {
  return <ProfileScreen />;
}
